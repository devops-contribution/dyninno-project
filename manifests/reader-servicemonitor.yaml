apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: reader-monitor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: reader-service
  endpoints:
    - port: http
      interval: 5s
      path: /metrics
